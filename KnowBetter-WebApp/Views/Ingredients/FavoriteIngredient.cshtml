@model KnowBetter_WebApp.Models.UserFavoriteIngredient

@{
    ViewData["Title"] = "FavoriteIngredient";
}

<h3>Your Favorite Ingredients</h3>


<table class="table">
    <tbody>
        @* foreach (var item in Model)
         {
           <tr>
             <td>
               @Html.DisplayFor(modelItem => item.UserFavoriteIngredientId)
             </td>
           </tr>
         }
            *@
    </tbody>
</table>

<div class="row">
    <div class="col-md-4">
        <form asp-action="AddFav">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserFavoriteIngredientId" class="control-label">Add Ingredient</label>
                <input asp-for="UserFavoriteIngredientId" class="form-control" />
                <span asp-validation-for="UserFavoriteIngredientId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>