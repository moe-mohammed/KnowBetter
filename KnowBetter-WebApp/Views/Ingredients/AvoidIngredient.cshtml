@model KnowBetter_WebApp.Models.UserAvoidIngredient

@{
    ViewData["Title"] = "AvoidIngredient";
}

<h3>Your Avoid Ingredients</h3>

<table class="table">
    <tbody>
        @* foreach (var item in Model)
         {
           <tr>
             <td>
               @Html.DisplayFor(modelItem => item.UserAvoidIngredientId)
             </td>
           </tr>
         }
        *@
    </tbody>
</table>

<!-- have user type in ingredient name
query database for ingredient name
return list of matches
user selects a match
user clicks add / optional
ingredient is now linked in avoid/favorite ingredient
that's all
-->

<div class="row">
    <div class="col-md-4">
        <form asp-action="AddAvoid">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <!--
                <label asp-for="IngredientName" class="control-label">Add Ingredient</label>
                <input asp-for="IngredientName" class="form-control"/>
                -->
                <!-- <span asp-validation-for="UserAvoidIngredientName" class="text-danger"></span> -->
            </div>
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>