@model KnowBetter_WebApp.Models.UserAvoidIngredient

@{
    ViewData["Title"] = "AvoidIngredient";
}

<h3>Your Avoid Ingredients</h3>


<table class="table">
    <tbody>
        @* foreach (var item in Model)
         {
           <tr>
             <td>
               @Html.DisplayFor(modelItem => item.UserAvoidIngredientId)
             </td>
           </tr>
         }
            *@
    </tbody>
</table>

<div class="row">
    <div class="col-md-4">
        <form asp-action="AddAvoid">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserAvoidIngredientId" class="control-label">Add Ingredient</label>
                <input asp-for="UserAvoidIngredientId" class="form-control" />
                <span asp-validation-for="UserAvoidIngredientId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>